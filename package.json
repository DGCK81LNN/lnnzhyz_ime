{
  "private": true,
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.18.0",
    "@typescript-eslint/parser": "^5.18.0",
    "esbuild": "^0.14.32",
    "eslint": "^8.12.0"
  },
  "scripts": {
    "build": "esbuild app.mjs --bundle --minify --sourcemap --outfile=site/bundle.min.js"
  }
}
